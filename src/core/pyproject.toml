[project]
name = "core"
version = "0.1.0"
description = ""
authors = [
    {name = "dinosaurien",email = "edvin.lolic@gmail.com"}
]
requires-python = ">=3.12"
dependencies = [
    "huggingface-hub (>=1.10.2,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
